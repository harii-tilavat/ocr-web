import { CanvasDirectiveDirective } from './canvas-directive.directive';

describe('CanvasDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
