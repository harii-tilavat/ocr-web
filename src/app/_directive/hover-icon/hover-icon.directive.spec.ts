import { HoverIconDirective } from './hover-icon.directive';

describe('HoverIconDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverIconDirective();
    expect(directive).toBeTruthy();
  });
});
