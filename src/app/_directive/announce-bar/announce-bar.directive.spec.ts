import { AnnounceBarDirective } from './announce-bar.directive';

describe('AnnounceBarDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnounceBarDirective();
    expect(directive).toBeTruthy();
  });
});
