import { ImgExtentionPipe } from './img-extention.pipe';

describe('ImgExtentionPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgExtentionPipe();
    expect(pipe).toBeTruthy();
  });
});
