import { MetadataFilterPipe } from './metadata-filter.pipe';

describe('MetadataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MetadataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
